class tablas{ //Son dos tablas a dibujar
  
 //PROPIEDADES
 // 1era tabla /* posX, posY, tam, VelX, velY*/
 // 2da tabla /* posX, posY, tam, VelX, velY*/
 
 
 //CONTRUCTOR
 //para la 1era tabla
 /* NO TIENE velocidad, posicion es siempre la misma, tamaño */
 
 // para la 2da tabla
 /* posicion controlada por el usuario (interactividad), velocidad intermedia, tamaño  */
 
 //METODOS
  /* DibujoT() rectangulo o img (para ambas tablas)
     moverUno() el rebote entre la pelotita y la tabla de abajo
     moverDos() el rebote de la pelotita con la tabla de arriba
    
  
  
  */
  
  
  
}
